import styled from "styled-components";

interface PensProps {

}

const StyledImg = styled.div`
    font-size: 2rem;
`

const Pens = ({}:PensProps):JSX.Element => {
    return (
        <StyledImg>✏️</StyledImg>
    )
}

export default Pens;